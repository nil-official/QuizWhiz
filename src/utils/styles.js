'use client';

/**
 * Common CSS styles that can be reused across components
 */
export const commonStyles = {
    tables: `
    table {
      border-collapse: collapse;
      margin: 15px 0;
      font-size: 14px;
    }
    th {
      background-color: #f2f2f2;
      border: 1px solid #ddd;
      padding: 8px;
      text-align: center;
    }
    td {
      border: 1px solid #ddd;
      padding: 8px;
    }
  `,

    questionContent: `
    .question-content table {
      border-collapse: collapse;
      margin: 15px 0;
      font-size: 14px;
    }
    .question-content th {
      background-color: #f2f2f2;
      border: 1px solid #ddd;
      padding: 8px;
      text-align: center;
    }
    .question-content td {
      border: 1px solid #ddd;
      padding: 8px;
    }
  `,
}